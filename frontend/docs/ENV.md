# Environment variables

Copy `frontend/.env.local.example` → `frontend/.env.local`.

- `NEXT_PUBLIC_API_BASE` (default `http://localhost:8080/api/v1`)
- `NEXT_PUBLIC_SITE_BASE` (default `http://localhost:3000`)

