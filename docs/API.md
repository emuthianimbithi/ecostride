# API

This file documents the API surface for EcoStride. Endpoints are grouped by module and follow `/api/v1`.

Refer to backend route registration in `backend/internal/common/http/routes.go` for the current scaffolded map.
